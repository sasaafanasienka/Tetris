// import { AXIOS_TEXT, CHANGE_TEXT } from "./types"

const initialBrick = {
    brick: [],
    baseLine: 0,
    baseColumn: 0
}

export const brickReducer = (state = initialBrick, action) => {
    switch(action.type) {
        default:
            return state
    }
}